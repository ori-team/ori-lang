oridoc 1

namespace ori.mem

doc module self
    summary:
        Standard library module `ori.mem` for Ori.
end

doc func region
    summary:
        Performs `region` operation on `ori.mem`.
    returns:
        The resulting `Region` value.
end

doc func reset
    summary:
        Performs `reset` operation on `ori.mem`.
    param r:
        The `r` parameter (Region).
end

doc func size
    summary:
        Performs `size` operation on `ori.mem`.
    param r:
        The `r` parameter (Region).
    returns:
        The resulting `int` value.
end

doc func count
    summary:
        Performs `count` operation on `ori.mem`.
    param r:
        The `r` parameter (Region).
    returns:
        The resulting `int` value.
end
