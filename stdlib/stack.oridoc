oridoc 1

namespace ori.stack

doc module self
    summary:
        Standard library module `ori.stack` for Ori.
end

doc func from_list
    summary:
        Performs `from list` operation on `ori.stack`.
    param items:
        The `items` parameter (list[T]).
    returns:
        The resulting `ori.stack.Stack[T]` value.
end

doc func peek_or
    summary:
        Performs `peek or` operation on `ori.stack`.
    param s:
        The `s` parameter (ori.stack.Stack[T]).
    param fallback:
        The `fallback` parameter (T).
    returns:
        The resulting `T` value.
end

doc func is_singleton
    summary:
        Performs `is singleton` operation on `ori.stack`.
    param s:
        The `s` parameter (ori.stack.Stack[T]).
    returns:
        The resulting `bool` value.
end
