# Ori language — syntax highlighting on GitHub
# Maps .orl files to Lua highlighting (closest match: end-delimited blocks,
# func/if/while/for keywords, -- comments). This is a temporary workaround
# until Ori is officially registered in github-linguist.
*.orl linguist-language=Lua
